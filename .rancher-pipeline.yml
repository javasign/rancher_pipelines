stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: rancher-demo
      pushRemote: true
      registry: 127.0.0.1:34458
- name: Deploy image into cluster
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
timeout: 60
